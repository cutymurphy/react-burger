const initialBuilder = {
    selectedIngredients: []
}

export const builderReducer = (state = initialBuilder, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}